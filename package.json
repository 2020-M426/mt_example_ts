{
  "license": "ISC",
  "scripts": {
    "compile": "tsc -p .",
    "lint": "tslint -c tslint.json  --project . -t codeFrame",
    "fix": "tslint -c tslint.json  --project . -t codeFrame --fix",
    "run": "yarn run compile ; node dist/*.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.12",
    "axios": "^0.21.0",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.2"
  }
}
